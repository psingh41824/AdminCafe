<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AdminCafe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AdminCafe" parent="Base.Theme.AdminCafe" />

    <style name="BottomNavigation">
        <item name="itemIconSize">22dp</item>
        <item name="itemIconTint">@drawable/bottom_nav</item>
        <item name="itemTextColor">@drawable/bottom_nav</item>
        <item name="labelVisibilityMode">labeled</item>
        <item name="itemTextAppearanceActive">@style/bottomStyle</item>
        <item name="itemTextAppearanceInactive">@style/bottomStyle</item>
    </style>

    <style name="bottomStyle" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>